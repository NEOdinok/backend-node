# Interview Questions — Vue, JS, TS

## 🧩 Vue.js

1. В каком жизненном цикле компонента правильно запрашивать данные во Vue — **до** или **после mount**?  
   А вообще корректна ли сама постановка вопроса?

2. Что такое `emit` во Vue?

3. Что такое `flush`?  
   Что такое `flush: 'deep'`?

4. Что такое **дистрибутивность** во Vue?

5. Что такое `provide` / `inject`?

6. Можно ли сделать `provide` колбэка на 10 уровней компонентов вниз и вызвать эту функцию из родителя?  
   Считается ли это нормальной практикой во Vue?

7. Какие есть **ограничения** у `provide/inject`?

8. Какие вообще во Vue есть **способы передачи данных** между компонентами?

9. Как определить, когда использовать **state-manager** (например, Pinia) или `provide/inject`?

10. Допустим, у нас компонент `Tab` (вкладка со своим контекстом):  
    стоит ли использовать для него отдельный контекст через `provide/inject` или стор?

11. Допустим, в приложении много страниц —  
    нужно ли создавать свой контекст `provide/inject` для каждой страницы?  
    Нужно ли создавать отдельный стор на каждую страницу или использовать общий глобальный?

12. Что такое `computed` и `watch`?  
    В чём разница?

13. Какие **флаги / опции** есть у `watch` и `computed`?  
    Что делает `immediate` (или `initial`) и зачем он нужен?

14. Что делает `nextTick` во Vue?  
    Как работает под капотом?  
    Работает на **микротасках** или **макротасках**?  
    Как `nextTick` гарантирует, что код выполнится **только после перерендера**?  
    Есть ли у него особое место в очереди задач?

15. Что такое `defineExpose` во Vue?  
    Работает ли он только внутри `<script setup>`?  
    В каких кейсах вообще нужен?

16. Можно ли "шарить" данные, передавая что-то через **слоты**?

---

## ⚙️ JavaScript

1. Задачи по **FizzBuzz** (со всеми вариациями) — напиши весь код.

2. Разница между `for...of` и `for...in`.

3. Преимущества `Map` перед объектом.  
   Когда `Map` лучше?

4. Задача про `query` (объединение объектов через spread) и все её вариации — в виде кода.

5. Что произойдёт, если в браузере ввести `google.com` и нажать Enter?

---

## 🧠 TypeScript

1. Что делает оператор `as`?

2. Что делает оператор `satisfies`?  
   Чем он отличается от `as`?

---

## 🌿 Vue Lifecycle

1. Назови **хуки жизненного цикла** во Vue.  
   (и для Options API, и для Composition API)
